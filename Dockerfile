# Используйте официальный образ Nginx как базовый образ
FROM nginx

# Установите рабочую директорию в /usr/share/nginx/html
WORKDIR /usr/share/nginx/html

# Копируйте содержимое вашего проекта в текущую директорию контейнера
COPY ./build .

# Порт, который будет прослушивать контейнер
EXPOSE 80

# Команда для запуска Nginx внутри контейнера при его старте
CMD ["nginx", "-g", "daemon off;"]
